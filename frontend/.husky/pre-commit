#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for code formatting and custom checks
npx lint-staged

# Run TypeScript compilation check (catches most critical errors)
npx tsc --noEmit
